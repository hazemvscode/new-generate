{
  "name": "new-generate",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "postinstall": "npm install --no-save @napi-rs/canvas-linux-x64-gnu || true",
    "fetch-font": "node downloadFont.js",
    "register-commands": "node deploy-commands.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.61",
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.2"
  }
}
